apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deegree-webservices.fullname" . }}-test-connection"
  labels:
    {{- include "deegree-webservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deegree-webservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
