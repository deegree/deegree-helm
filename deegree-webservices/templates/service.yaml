apiVersion: v1
kind: Service
metadata:
  name: {{ include "deegree-webservices.fullname" . }}
  labels:
    {{- include "deegree-webservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deegree-webservices.selectorLabels" . | nindent 4 }}
